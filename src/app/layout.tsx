import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Trancelle International Academy | Online Examination Portal",
  description:
    "Official online examination portal of Trancelle International Academy. Students can securely access examinations and administrators can manage exams, students, and results.",
  keywords: [
    "Trancelle International Academy",
    "Trancelle Exam Portal",
    "Trancelle Examination Portal",
    "online examination portal",
    "student examination portal",
  ],
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
